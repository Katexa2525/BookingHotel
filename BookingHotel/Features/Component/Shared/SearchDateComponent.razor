@inject NavigationManager NavManager

<div class="container text-center mb-2">
  <div class="row justify-content-start">
    <div class="col">
      <input @bind="SelectedStartDate" type="date" class="form-control">
    </div>
    <div class="col">
      <input @bind="SelectedEndtDate" type="date" class="form-control">
    </div>
    <div class="col">
      <InputNumber @bind-Value="CountAdult" class="form-control" id="adult" />
    </div>
    <div class="col">
      <InputNumber @bind-Value="CountChildren" class="form-control" id="adult"/>
    </div>
    <div class="col">
      <button class="btn btn-primary" title="Поиск на даты..." @onclick="@(() => DoClick() )">
        <i class="bi bi-binoculars"></i>
        Найти
      </button>
    </div>
  </div>
</div>

@code {

  [Parameter]
  public DateTime SelectedStartDate { get; set; } = DateTime.Now;

  [Parameter]
  public DateTime SelectedEndtDate { get; set; } = DateTime.Now.AddDays(1);

  [Parameter]
  public int CountAdult { get; set; }

  [Parameter]
  public int CountChildren { get; set; }

  private void DoClick() => Console.WriteLine("DoClick");
}
